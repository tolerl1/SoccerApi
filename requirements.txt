aiofiles
sqlalchemy
aiohttp
fastapi
uvicorn[standard]
alembic
pydantic>=2.0
# Optional — install as needed:
# soccerdata          # FBref, Understat, Club Elo scrapers
# scikit-learn        # trainable GP model (app/xgc_event/goal_probability.py)
# numpy               # required by scikit-learn path